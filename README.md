# qrtools

A Python library and CLI tool for generating QR codes from arbitrary text data.

## Features
- Generate QR codes from URLs, text, JSON, device IDs, etc.
- Automatic URL normalization and sanitization
- Customizable output size, border, and module dimensions
- Smart filename generation based on input content
- CLI interface with argument parsing

## Installation
